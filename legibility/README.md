# Application / Corpus Legibility

This directory records how agents should inspect the harness source repo as an
application and as a corpus of process-domain infrastructure.

Use `inventory.yaml` for structured inspection surfaces. Use `notes.md` for
repo-specific inspection notes that are useful but not yet worth structuring.
