# Reports And Retrieval

This directory records local report definitions and durable report snapshots
for an installed harness target.

Use `reports/catalog.yaml` for structured report definitions. Use
`reports/snapshots.md` for human-readable snapshots or notes that should
remain local to this repo.

Run:

```bash
harness reports list
harness reports check
harness reports report
harness reports generate
```
