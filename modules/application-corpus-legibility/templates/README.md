# Application / Corpus Legibility

This directory records how agents should inspect the repo's actual subject
matter.

Use `inventory.yaml` for structured application, runtime, validation, fixture,
log, source-map, generated-report, or corpus inspection surfaces. Use
`notes.md` for repo-specific inspection notes that are useful but not yet worth
structuring.
